# Copy to .env and edit. Sourced by deploy.sh if present.

# Base dir Caddy serves from.
DEPLOY_PATH=/srv/bluefox.cafe

# Tailnet hostname of the VPS. Leave unset (or comment out) to publish
# locally to DEPLOY_PATH on the current host instead of over SSH.
DEPLOY_HOST=vps
